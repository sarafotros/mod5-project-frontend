import { makeStyles } from '@material-ui/core/styles';

const useStyles = makeStyles({
	Categories: {
		height: 150,
		display: 'flex',
        flexDirection: 'row',
        margin: '10px 0 20px'
    },
    CategoriesSection: {
        marginTop: 18,
    }
});
export default useStyles;